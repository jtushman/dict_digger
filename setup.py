from setuptools import setup

setup(name='dict_digger',
      version='0.2.1',
      description='Useful syntax for digging into nested dictionaries, lists and tuples, and removes the need to check if a key exists',
      url='https://github.com/jtushman/dict_digger',
      author='Jonathan Tushman',
      author_email='jonathan@zefr.com',
      license='MIT',
      test_suite='test_dict_digger',
      packages=['dict_digger'],
      zip_safe=False)
